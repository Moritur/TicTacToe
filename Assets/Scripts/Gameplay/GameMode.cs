namespace TicTac.Gameplay
{
    /// <summary>Game mode in which round can be played.</summary>
    public enum GameMode
    {
        /// <summary>Local multiplayer.</summary>
        PlayerVsPlayer,
        PlayerVsEasyAI,
        PlayerVsMediumAI
    }
}
